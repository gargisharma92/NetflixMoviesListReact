import React from "react";


const Link = (props) => {
    return <a href={props.link} target="_blank" rel="noreferrer"  className="btn btn-primary"><button>Watch Now</button></a>
}
export default Link;
